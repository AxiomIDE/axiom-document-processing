name: christian/axiom-document-processing
version: 0.1.0
language: python
description: Document processing nodes for parsing PDFs and splitting text into overlapping chunks.
author: Axiom
license: MIT
nodes:
    - name: PDFDownloader
      input: DownloadRequest
      output: PDFInput
      description: Downloads a PDF from a URL and returns the raw bytes ready for parsing.
    - name: PDFParser
      input: PDFInput
      output: DocumentText
      description: Extracts all plain text from a PDF document provided as raw bytes.
    - name: TextChunker
      input: ChunkRequest
      output: ChunkItem
      type: pipeline
      description: Splits a text string into overlapping fixed-size chunks and streams one ChunkItem per chunk.
